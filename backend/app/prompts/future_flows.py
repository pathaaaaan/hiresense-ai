"""
Prompt builders for the flows NOT included in this slice
(interview question generation, answer evaluation, skill-gap/roadmap).

Left as stubs so the next slice can drop straight into this architecture:
add a schema in app/schemas, a service in app/services, a route in app/api,
and a prompt builder here, following the same pattern as
resume_analyzer.py / ats_evaluator.py.
"""

# def build_interview_generator_prompt(resume_skills, target_role): ...
# def build_answer_evaluator_prompt(question, answer, role): ...
# def build_skill_gap_prompt(resume_skills, target_role): ...
